# ==============================================================================
# Cross-lingual KG Completion — Local (Windows, CPU-only) Dependencies
# ==============================================================================
# 
# INSTALLATION ORDER MATTERS on Windows. Follow these steps exactly:
#
# Step 1: Create conda environment
#   conda create -n crosslingual_kg python=3.10 -y
#   conda activate crosslingual_kg
#
# Step 2: Install PyTorch (CPU-only)
#   pip install torch==2.2.0 --index-url https://download.pytorch.org/whl/cpu
#
# Step 3: Install PyTorch Geometric scatter/sparse/cluster wheels
#   pip install torch_scatter torch_sparse torch_cluster -f https://data.pyg.org/whl/torch-2.2.0+cpu.html
#
# Step 4: Install PyTorch Geometric
#   pip install torch-geometric==2.5.2
#
# Step 5: Install all remaining dependencies
#   pip install -r requirements.txt
#
# NOTE: Do NOT run `pip install -r requirements.txt` without completing steps 2-4 first.
# PyTorch Geometric on Windows requires pre-built wheels from the PyG index.
# ==============================================================================

# --- Core ML ---
# torch==2.2.0  # Installed separately (Step 2) — DO NOT uncomment
# torch-geometric==2.5.2  # Installed separately (Step 4) — DO NOT uncomment

# --- NLP / Embeddings ---
sentence-transformers==2.7.0
transformers==4.40.0

# --- Data / Scientific Computing ---
SPARQLWrapper==2.0.0
networkx==3.2.1
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.1

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
jinja2==3.1.4
python-multipart==0.0.9

# --- Configuration / Env ---
pyyaml==6.0.1
python-dotenv==1.0.1

# --- Utilities ---
tqdm==4.66.4
loguru==0.7.2

# --- Testing ---
pytest==8.2.0
pytest-cov==5.0.0

# --- Optional: fuzzy string matching for entity lookup ---
thefuzz==0.22.1
python-Levenshtein==0.25.1
